import GLOBAL_STYLES from '../../Theme/GlobalStyles';

export const styles = {
  container: {
    backgroundColor: `${GLOBAL_STYLES.color.mainColorDarkBlue}`,
    position: 'fixed',
    left: 0,
    bottom: 0,
    width: '100%',
    display: 'flex'
  }
};
