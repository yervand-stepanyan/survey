import GLOBAL_STYLES from '../../../Theme/GlobalStyles';

export const styles = {
  developersSection: {
    display: 'flex',
    flexDirection: 'column',
    padding: `${GLOBAL_STYLES.spacing.small}`
  }
};
